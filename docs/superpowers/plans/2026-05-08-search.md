# Search Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a `/search` page that lets family members find old posts by keyword using MySQL FULLTEXT search, displaying compact results rows with highlighted match snippets.

**Architecture:** A new `src/routes/search.js` handles GET /search, adds a FULLTEXT migration to db.js, and renders `src/views/search.ejs`. Results are ranked by MySQL relevance score. Nav links are added to both the desktop nav and mobile drawer in `partials/nav.ejs`.

**Tech Stack:** Node.js/Express, EJS, MySQL2 (`MATCH ... AGAINST` fulltext), no client-side JS beyond existing app.js.

---

## File Map

| Action | File |
|--------|------|
| Modify | `src/db.js` — add FULLTEXT migration |
| Create | `src/routes/search.js` — route + snippet helper |
| Create | `src/views/search.ejs` — search page view |
| Modify | `src/app.js` — register `/search` route |
| Modify | `src/views/partials/nav.ejs` — add Search link to desktop + drawer |

---

## Task 1: Add FULLTEXT index migration

**Files:**
- Modify: `src/db.js`

- [ ] **Step 1: Add the migration**

In `src/db.js`, find the `migrations` array (around line 182). Add this entry at the **end** of the array, after the last existing entry (`ALTER TABLE notifications ADD INDEX idx_user_read ...`):

```js
`ALTER TABLE posts ADD FULLTEXT INDEX ft_posts (title, content)`,
```

The final entries of the array should look like:

```js
    `ALTER TABLE notifications ADD INDEX idx_user_read (user_id, read_at)`,
    `ALTER TABLE posts ADD FULLTEXT INDEX ft_posts (title, content)`,
  ];
  for (const q of migrations) {
    try { await pool.query(q); } catch { /* column already exists */ }
  }
```

- [ ] **Step 2: Verify the migration runs**

```bash
cd /home/jmull/projects/family-news
node -e "require('dotenv').config(); const {initDb} = require('./src/db'); initDb().then(() => { console.log('OK'); process.exit(0); }).catch(e => { console.error(e.message); process.exit(1); })"
```

Expected output: `OK`

Then confirm the index exists:

```bash
sudo docker exec mysql mysql -u root -pc0f62d7a499670fe2c1a159b1e9679a6 family_news -e "SHOW INDEX FROM posts WHERE Key_name = 'ft_posts';" 2>/dev/null
```

Expected: one row with `Key_name: ft_posts`, `Index_type: FULLTEXT`.

- [ ] **Step 3: Commit**

```bash
git add src/db.js
git commit -m "feat(search): add FULLTEXT index on posts(title, content)"
```

---

## Task 2: Create the search route

**Files:**
- Create: `src/routes/search.js`

- [ ] **Step 1: Create the file**

Create `src/routes/search.js` with this content:

```js
// Search posts by keyword using MySQL FULLTEXT natural language mode.
const express = require('express');
const router = express.Router();
const { pool } = require('../db');
const { requireAuth } = require('../middleware/auth');

const MIN_QUERY_LEN = 2;

// Returns an HTML-safe snippet of `content` with the first occurrence of
// `query` wrapped in <mark> tags. Strips @[Name](id) mention tokens first.
function buildSnippet(content, query) {
  const plain = content.replace(/@\[([^\]]+)\]\(\d+\)/g, '$1');
  const lower = plain.toLowerCase();
  const term = query.trim().toLowerCase();
  const idx = lower.indexOf(term);

  let start = 0;
  let end = Math.min(150, plain.length);
  if (idx !== -1) {
    start = Math.max(0, idx - 60);
    end = Math.min(plain.length, idx + term.length + 90);
    if (start > 0) {
      const ws = plain.indexOf(' ', start);
      if (ws !== -1 && ws < idx) start = ws + 1;
    }
    if (end < plain.length) {
      const ws = plain.lastIndexOf(' ', end);
      if (ws > idx + term.length) end = ws;
    }
  }

  const prefix = start > 0 ? '…' : '';
  const suffix = end < plain.length ? '…' : '';
  let snippet = plain.slice(start, end)
    .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;');

  const escaped = term.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
  snippet = snippet.replace(new RegExp(escaped, 'gi'), '<mark>$&</mark>');

  return prefix + snippet + suffix;
}

router.get('/', requireAuth, async (req, res) => {
  const q = (req.query.q || '').trim();

  if (q.length < MIN_QUERY_LEN) {
    return res.render('search', { query: q, results: null, error: null });
  }

  try {
    const userId = req.session.user.id;
    const [rows] = await pool.query(`
      SELECT p.id, p.title, p.content, p.created_at, p.user_id,
             u.name AS author_name, u.avatar_url AS author_avatar,
             MATCH(p.title, p.content) AGAINST(? IN NATURAL LANGUAGE MODE) AS score
      FROM posts p
      JOIN users u ON p.user_id = u.id
      WHERE p.deleted_at IS NULL
        AND (p.publish_at IS NULL OR p.publish_at <= NOW() OR p.user_id = ?)
        AND MATCH(p.title, p.content) AGAINST(? IN NATURAL LANGUAGE MODE)
      ORDER BY score DESC
      LIMIT 50
    `, [q, userId, q]);

    const results = rows.map(r => ({
      ...r,
      snippet: buildSnippet(r.content, q),
    }));

    res.render('search', { query: q, results, error: null });
  } catch (err) {
    console.error('Search error:', err);
    res.render('search', { query: q, results: null, error: 'Search is temporarily unavailable.' });
  }
});

module.exports = router;
```

- [ ] **Step 2: Verify the file parses**

```bash
node -e "require('./src/routes/search')" && echo "OK"
```

Expected: `OK` (no errors).

- [ ] **Step 3: Commit**

```bash
git add src/routes/search.js
git commit -m "feat(search): add search route with FULLTEXT query and snippet builder"
```

---

## Task 3: Register the route and add nav links

**Files:**
- Modify: `src/app.js`
- Modify: `src/views/partials/nav.ejs`

- [ ] **Step 1: Register in app.js**

In `src/app.js`, find the route registrations block (around line 111–124). Add the search route after the notifications line:

```js
app.use('/notifications', require('./routes/notifications'));
app.use('/search', require('./routes/search'));
```

- [ ] **Step 2: Add Search to the desktop nav**

In `src/views/partials/nav.ejs`, find the desktop nav links block inside `.fn-nav-links`. The existing links end with Photos → Feedback → Notifications → What's New → Profile → Sign out. Add Search before Photos:

```html
        <a href="/photos" class="fn-nav-link">Photos</a>
```

Replace with:

```html
        <a href="/search" class="fn-nav-link">Search</a>
        <div class="fn-nav-sep"></div>
        <a href="/photos" class="fn-nav-link">Photos</a>
```

- [ ] **Step 3: Add Search to the mobile drawer**

In `src/views/partials/nav.ejs`, find the drawer nav block (`.fn-drawer-nav`). The existing links are Feed → Photos → Profile → Feedback → Notifications → What's New. Add Search after Feed:

```html
    <a href="/" class="fn-drawer-link">Feed</a>
    <a href="/photos" class="fn-drawer-link">Photos</a>
```

Replace with:

```html
    <a href="/" class="fn-drawer-link">Feed</a>
    <a href="/search" class="fn-drawer-link">Search</a>
    <a href="/photos" class="fn-drawer-link">Photos</a>
```

- [ ] **Step 4: Commit**

```bash
git add src/app.js src/views/partials/nav.ejs
git commit -m "feat(search): register route and add Search nav links"
```

---

## Task 4: Create the search view

**Files:**
- Create: `src/views/search.ejs`

- [ ] **Step 1: Create the view**

Create `src/views/search.ejs` with this content:

```html
<%- include('partials/head', { title: 'Search' }) %>
<%- include('partials/nav') %>

<%
function formatDate(date) {
  const d = new Date(date);
  const now = new Date();
  const sameYear = d.getFullYear() === now.getFullYear();
  return d.toLocaleDateString('en-US', {
    month: 'short', day: 'numeric',
    year: sameYear ? undefined : 'numeric'
  });
}
%>

<style>
  mark {
    background: #fbbf24;
    color: inherit;
    border-radius: 2px;
    padding: 0 2px;
  }
  .dark mark {
    background: #92400e;
    color: #fef3c7;
  }
</style>

<div class="max-w-2xl mx-auto px-4 py-6">
  <h1 class="text-xl font-bold text-slate-800 dark:text-slate-100 mb-4">Search</h1>

  <form method="GET" action="/search" class="flex gap-2 mb-6">
    <input
      type="search"
      name="q"
      value="<%= query %>"
      placeholder="Search posts by keyword…"
      autofocus
      class="flex-1 rounded-xl border border-slate-200 dark:border-slate-600 bg-white dark:bg-slate-800 text-slate-800 dark:text-slate-100 px-4 py-2.5 text-base focus:outline-none focus:ring-2 focus:ring-brand-400"
    >
    <button
      type="submit"
      class="px-5 py-2.5 bg-brand-600 hover:bg-brand-700 text-white rounded-xl font-medium text-sm transition-colors"
    >Search</button>
  </form>

  <% if (error) { %>
    <p class="text-sm text-red-500 dark:text-red-400"><%= error %></p>

  <% } else if (results === null) { %>
    <%# No query submitted — empty state, nothing to show %>

  <% } else if (results.length === 0) { %>
    <p class="text-sm text-slate-500 dark:text-slate-400">
      No posts found for "<%= query %>". Try a different word.
    </p>

  <% } else { %>
    <p class="text-sm text-slate-500 dark:text-slate-400 mb-4">
      <%= results.length %> result<%= results.length === 1 ? '' : 's' %> for "<%= query %>"
    </p>

    <div class="space-y-3">
      <% results.forEach(function(r) { %>
      <a href="/post/<%= r.id %>" class="flex items-start gap-3 bg-white dark:bg-slate-800 rounded-2xl border border-slate-200 dark:border-slate-700 p-4 hover:bg-slate-50 dark:hover:bg-slate-700 transition-colors no-underline block">
        <% if (r.author_avatar) { %>
        <img src="<%= r.author_avatar %>" class="w-9 h-9 rounded-full object-cover flex-shrink-0 mt-0.5" alt="">
        <% } else { %>
        <div class="w-9 h-9 rounded-full bg-brand-100 dark:bg-brand-600/20 flex items-center justify-center text-brand-600 dark:text-brand-400 font-semibold text-sm flex-shrink-0 mt-0.5">
          <%= r.author_name.charAt(0).toUpperCase() %>
        </div>
        <% } %>
        <div class="flex-1 min-w-0">
          <div class="flex items-baseline justify-between gap-2 mb-1">
            <span class="text-sm font-semibold text-slate-800 dark:text-slate-100 truncate"><%= r.author_name %></span>
            <span class="text-xs text-slate-400 dark:text-slate-500 flex-shrink-0"><%= formatDate(r.created_at) %></span>
          </div>
          <% if (r.title) { %>
          <p class="text-sm font-semibold text-slate-700 dark:text-slate-200 mb-1"><%= r.title %></p>
          <% } %>
          <p class="text-sm text-slate-500 dark:text-slate-400 leading-snug"><%- r.snippet %></p>
        </div>
      </a>
      <% }) %>
    </div>
  <% } %>
</div>

<script src="/js/app.js"></script>
</body>
</html>
```

- [ ] **Step 2: Verify the view renders**

Restart the app and confirm the search page loads:

```bash
sudo docker compose -f /home/jmull/docker/docker-compose.yml restart family-news 2>/dev/null || node src/app.js &
sleep 2
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/search -H "Cookie: <your-session-cookie>"
```

Expected: `200`

(For a quick local check without a session cookie, look for no Node.js syntax errors in the restart output.)

- [ ] **Step 3: Commit**

```bash
git add src/views/search.ejs
git commit -m "feat(search): add search page view"
```

---

## Task 5: Smoke test and deploy

- [ ] **Step 1: Push to trigger deploy**

```bash
git push
```

- [ ] **Step 2: Watch the GitHub Actions build**

```bash
gh run list --repo Jonathan-Mullet/family-news --limit 3
```

Wait for both `build` and `deploy` jobs to show ✓. The self-hosted Pi runner deploys automatically on push to main.

- [ ] **Step 3: Smoke test the live site**

Visit `https://news.jonathan-mullet.com/search` and verify:

1. **Empty state:** Page loads, search box is focused, no results shown
2. **Search with results:** Enter a keyword that appears in a post (e.g., a family member name or recent topic). Confirm results show with author, date, and highlighted snippet.
3. **No results:** Enter a nonsense word (e.g. "xyzzy"). Confirm "No posts found…" message.
4. **Nav:** Confirm "Search" link appears in desktop nav and mobile hamburger drawer.
5. **Dark mode:** Toggle dark mode, confirm `<mark>` highlight uses amber-on-dark styling.
6. **Link:** Click a result row, confirm it navigates to the correct post.
