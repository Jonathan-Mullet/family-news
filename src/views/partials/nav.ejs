<nav class="bg-white border-b border-slate-200 sticky top-0 z-10 shadow-sm">
  <div class="max-w-2xl mx-auto px-4 py-3 flex items-center justify-between">
    <a href="/" class="font-bold text-brand-600 text-lg tracking-tight">📰 Family News</a>
    <% if (user) { %>
    <div class="flex items-center gap-4 text-sm">
      <span class="text-slate-500 hidden sm:block"><%= user.name %></span>
      <% if (user.role === 'admin') { %>
      <a href="/admin" class="text-slate-500 hover:text-brand-600 transition-colors">Admin</a>
      <% } %>
      <a href="/logout" class="text-slate-500 hover:text-red-500 transition-colors">Sign out</a>
    </div>
    <% } %>
  </div>
</nav>
